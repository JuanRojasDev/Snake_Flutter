import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class Snake_Image extends StatelessWidget {

  Snake_Image();


  @override
  Widget build(BuildContext context) {
    return Row(
      children: <Widget>[
        Text("Nombre"),
      ],
    );
  }
}