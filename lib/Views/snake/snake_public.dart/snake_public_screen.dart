import 'package:flutter/material.dart';

class SnakePublicScreen extends StatelessWidget {
  const SnakePublicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}